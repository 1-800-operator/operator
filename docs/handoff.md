# Session 207 handoff (2026-05-07) — 14.20.5 system-audio leg validated end-to-end; uv-tool-install staleness diagnosed

Diagnostic-only session, **zero code changes by this agent**. User ran a slip live-test (phone in another room playing audio, headphones on, mic muted) to validate S206's notarized helper actually delivers system audio frames. Final answer: it works — `[S]` callbacks streaming, `whisper_done "First thing, one, two, three."` and `"That's been testing."` landed in the meeting JSONL on the successful run at 17:06:30+. **Got there only after diagnosing a wrong-binary bug masquerading as TCC cache staleness:** `~/.local/share/uv/tools/1-800-operator/` had a pre-S206 resolver pointing at the older standalone helper (`com.operator.audio-capture`, ad-hoc) instead of the post-S206 notarized `.app` (`com.1-800-operator.audio-capture`). Three rounds of `tccutil reset` / `sudo killall tccd` / System Settings inspection were wasted because the binary actually launched had no relationship to the TCC entry being reset. Fix was `uv tool install --force --reinstall`. New Hard-Won Knowledge entry captured at top of `docs/agent-context.md`. Three product TODOs surfaced and parked as new Phase 14.21 in `docs/roadmap.md` — mic-mute privacy gate (highest priority — anything captured while muted is reachable via `@claude what did I just say`), TCC pre-prompt at first-run instead of first sound, and confirmed whisper-base matches voice-preserved exactly so the "Hosting" mishearing is character-not-regression.

## Exact next step (session 208)

User completes the second leg of the E2E test: come back to the desk, unmute mic, speak as a "reply," then `@claude what did the other person say?` to close the MCP round-trip. Mic stream is already proven working in every prior failure run (helper logs `[M]` callbacks streaming reliably across all attempts) — the genuinely-untested part is the inner-claude transcript-MCP call answering correctly from the captions the system-audio leg just produced. After that: (a) pick up Phase 14.21.1 (mic-mute privacy gate, ~3h, real privacy bug — Meet's mute toggle does not silence the helper's `M` stream because the helper taps the system mic device directly), (b) Phase 14.21.2 (TCC pre-prompt UX + doctor probes for both perms, ~2h), (c) characterize and commit the parallel-agent working-tree refactor (see Don't Forget below), or (d) push the three local commits S206 left ahead of `origin/main` (`51b69f3`, `a0aa727`, `66ab455`).

## Open questions / blockers

- **Apple Dev cert** — was in flight from S198, gating 14.20.5. **14.20.5 is now substantively done with today's notarized .app**, so the cert that S198 was waiting on has clearly been delivered. Carry-forward note can be retired.
- **API key rotation** still pending from S205 second-pass — user reminded to revoke ANTHROPIC + OPENAI keys at console; not blocking, hygiene only.
- **`/ultrareview` + `/security-review`** remain gated on the mic-leg validation per memory — slip captions were the gate, system-audio leg is now validated, mic leg remains.

## Don't forget

- **Massive uncommitted refactor in the working tree NOT from S207.** `git status` at end-session shows ~1170 LOC delete in `pipeline/permission_chat_handler.py` + `pipeline/permission_bridge.py` + `pipeline/confirmation.py` + their tests, plus modifications in `attach_adapter.py` + `chat_runner.py` + `claude_cli.py`. Begin-session `git status` snapshot showed only `M docs/handoff.md`, so all of this happened DURING the conversation — but I made zero code edits this session. Almost certainly a parallel agent the user ran concurrently while we were debugging audio. **Deliberately not committed** — wrong author would attach a misleading message. S208 should ask the user which agent did this work and have that agent (or the user) characterize and commit.
- **Three S206 commits still unpushed**: `51b69f3` (orphan delete), `a0aa727` (docs sweep), `66ab455` (14.20.5 ship). The S206 handoff said "push first thing in S207"; this session never got around to it because we went straight into the live-test diagnostic.
- **Untracked working-tree items carried from S206 untouched again**: `debug/14_20_audio_spike/{0, DECISION.md, STT_COMPARISON.md, USER_NOTE.md, decode_frames.py, spike_capture, spike_capture.swift}`, `debug/resume_spike/`, `docs/landing-page.md`, `public/`. Carry over.
- **Helper's tccd-cache-stale warning is a false-positive magnet** — see the new Hard-Won Knowledge entry. The "WARN — system audio: 0 callbacks in 10s (likely tccd cache stale)" line in `swift/operator-audio-capture.swift` primed three rounds of TCC chasing when the real cause was a wrong-binary identity mismatch. Worth rewording in the helper to "check that the binary identity matches the granted entry" the next time someone touches that file.
- **mic stream is already proven** — every failed run logged `[M]` callbacks at 127KB/2s. Don't re-debug the mic path; the only untested piece on the mic side is the chat round-trip via the transcript MCP.
