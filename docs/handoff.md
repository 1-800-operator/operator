# Session 228 handoff — 14.22 refactor sections G/I/J/K landed (branch `phase-14.22-pty-pivot`)

## What happened this session

Continued the PTY+hooks production refactor. The big move was a **mid-course reshape of section G**: the original "operator narrates Claude's tool calls off a `tools.jsonl` hook tail" was built, live-tested, and torn back out — the `running Bash: <raw command>` lines were cryptic and `PostToolUseFailure` was mis-narrated as a permission denial. The replacement: **Claude self-narrates in its own `[🤖 Claude]` voice**, prompted by an operator-authored **first-paste briefing** (`ClaudeCLIProvider._BRIEFING`, turn 0, reply consumed), delivered in real time by **tailing the Claude Code transcript JSONL** during the turn (which also restored the streaming the early PTY pivot lost). This narrowed the naked-spawn invariant — it constrains spawn *flags*, not a first-turn briefing paste — and the `[☎️ Operator]` switchboard voice was **purged entirely** (`REPLY_PREFIX_OPERATOR`, `send_chat_raw`, the `raw` send path — all gone; the bot is just Claude). Also landed: **#4** inner-claude respawn fix (stale `ready.flag` + leaked threads), an **audit** that caught a real protocol-error silent-failure bug, **section I** foreign-hook detector (`"Stop hook feedback:"` detection via the transcript tail → `ProviderResponse.notices` → `[🤖 Claude]` chat line; validated against a real `decision: block` transcript), **section J** (reshaped to a 3-line clean-exit tweak — transcript-tailing already solved the real lost-reply risk), and **section K** Claude Code version floor (≥ 2.1.141, gated in `_probe_claude_code`). operator-plugin bumped to **0.1.14** (pushed) — dead `pretool.sh`/`error.sh` hooks removed. ~14 commits on `phase-14.22-pty-pivot`, all **unpushed**.

## Exact next step

**Section L** — the last 14.22 refactor item. Two parts: (1) plugin install smoke test — verify `hooks/scripts/*.sh` exec bits survive `uv tool install --reinstall` + desktop-app plugin sync (`hooks.json` already uses interpreter-explicit `bash $CLAUDE_PLUGIN_ROOT/...`, so likely a one-line verification); (2) **rewrite `test_claude_cli_provider.py`** — it still asserts the gone per-shellout `--resume` behavior and is the one failing test in the suite. The rewrite should cover the new architecture: briefing turn-0 consumption, transcript-tailing, respawn-after-crash, foreign-hook detection. Then **merge `phase-14.22-pty-pivot` → `main`** and run the DECISION.md 20–25 integration pass.

## Open items / blockers

- **Branch docs are stale vs. main.** This branch forked at S226's end-session, so `docs/roadmap.md` + `docs/agent-context.md` don't carry S227's main-side sweep, and `debug/model-log.md` isn't in this worktree at all. Logging changed substantially this session (new `TIMING` fields `reply_blocks`/`foreign_hook`, briefing/foreign-hook log lines, removed narration chat-sends). Docs reconciliation + model-log update both have to happen at the merge.
- **June 15 classification unknown** still sits under everything (interactive-PTY-as-subscription is untested until then).
- The `--dangerously-skip-permissions` + foreign-hook write hazard (a foreign Stop hook whose `reason` reads as an instruction → inner-claude acts on it; during testing it edited the user's global `~/.claude/settings.json`, since reverted) is captured in agent-context Hard Won Knowledge — relevant to the project threat model, not a blocker.
