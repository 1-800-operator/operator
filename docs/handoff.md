# Session 178 handoff (2026-05-01) — Code-quality audit tier-1 sweep complete

This session walked the new code-quality audit (`docs/code-quality-audit-session-178.md`) — a persnickety reviewer-style pass over ~11k LOC of runtime that surfaced 12 PR-blockers (Tier 1) plus 10 structural items (Tier 2) plus ~70 deferred nits. Tier 1 was worked one finding at a time, pausing to re-verify framing against the actual code at each step. **9 of 12 Tier 1 findings RESOLVED**, 3 deferred with documented rationale, all landed in one commit (`500a3bc`). Headline wins: collapsed `personality`+`ground_rules` into a single `system_prompt` field across schema/loader/wizard/4 bundled YAMLs/tests/docs (T1.2); built a unified `_narrate_failure` helper that hands LLM-call failures back to the LLM via a no-tools-no-record-no-retry call asking for a plain-text apology, falling through to a hardcoded message on second-failure (T1.4 / T1.5 / T1.12 — solves the silent-fail-on-429 in the main loop); fixed mid-stream-429 double-post in `anthropic.complete_streaming` (T1.6); deleted dead `canonical_text` reconstruction in `claude_cli.complete_streaming` (T1.7 — audit framing was wrong, finding was actually unused code); shared `is_yes` helper for both confirmation surfaces (T1.9); mirrored macOS participant signal to `LinuxAdapter` so `saw_others`/1-on-1/alone-grace actually fire on Linux (T1.10); moved `OPERATOR_BOT` env var to the first line of `_run_bot` so contract is enforced by code position not comment (T1.1). 4 new test files (`test_narrate_failure.py` 12 tests, `test_confirmation.py` 12, `test_linux_adapter_participants.py` 10, plus 2 streaming-429 tests in `test_anthropic_provider.py`); all previously-passing suites stay green. Branch is 33 commits ahead of `origin/main`.

**Exact next step (session 179): Phase 14.13.3** — flip install copy + marketing references to `1-800-operator.com`; redo the 14.6 DNS work (Cloudflare nameservers) on the new domain. Install one-liner becomes `curl -sSf https://1-800-operator.com/install | sh`. Then **Phase 16** (README rewrite, 30s demo GIF, single-page landing site at `1-800-operator.com`, launch campaign).

**Open carry-overs:**
1. **PyPI version bump.** Distribution name `1-800-operator` is pinned in `pyproject.toml`; placeholder package on PyPI is at v0.0.1. Version-bump + confirm placeholder origin/maintainer story before publishing.
2. **T1.3 (Linear `limit`-strip duplication)** deferred. Two sites doing the same job (`chat_runner.py:736-738` + `mcp_client.py:366-368`); no user impact today. Revisit alongside T2.5 (server-specific knowledge in neutral abstractions) in the broader cleanup.
3. **T1.8 (in-memory vs disk `MeetingRecord.tail()` semantics)** deferred. `slug=None` mode used only by `operator try` and tests, not production. Test fidelity concern only.
4. **T1.11 (Chrome vs Chromium profile path on macOS)** deferred. Comment in `google_signin.py:197-211` documents the deferred fix conditions (Google moves auth cookies to keychain-encrypted slot, or Chrome bumps profile-DB schema beyond Chromium-for-Testing). Revisit on first observed runtime auth failure. One-line fix at that point: `executable_path=str(CHROME_PATH)` in the adapter's `launch_persistent_context`.
5. **Tier 2 audit (10 items) + appendix nits (~70)** in `docs/code-quality-audit-session-178.md` are unbroached. Some may be cheap wins; most are structural and post-launch.
6. **Carry-overs from S177:** `_do_send_chat` ID-readback race against multi-participant chat; `MeetingRecord.append` divergence between memory and disk on transient OSError; `_on_tool_use` docstring drift (one-line fix on next chat_runner edit).
