# Session 183 handoff (2026-05-03) — strategic pivot from OSS CLI to closed-source two-product trajectory

This was a strategy session. **No code touched, one commit on local main (`beff05b`).** Pivoted from the OSS Python CLI (Phase 14.13 → 16 trajectory) to a closed-source two-product roadmap: **v1 Operator** = cloud switchboard at `1-800-operator.com` + Claude Code plugin (`claude plugin install operator`) bridging meeting events to user's local Claude Code via WebSocket; **v2 Brainchild** = desktop-native build-your-own-bot product, gated on v1 validation. BYO-LLM (we never see Anthropic bills). Self-hosted headless Chrome on Fly.io (~$0.10–$0.25/meeting-hour). Mac-only, Meet-only, Claude-Code-only at launch — Codex parity is Phase A7 with two locked invariants in `docs/product-strategy.md` (caption parity via Codex plugin manifest; MCP inheritance via plugin-runs-inside-Codex). Three docs swept: `docs/product-strategy.md` rewritten as the load-bearing forward doc (gitignored, local-only — 265 lines); `docs/roadmap.md` and `docs/agent-context.md` got pivot markers prepended with full OSS-phase history preserved below. Three spikes ruled out alternatives: Anthropic's OpenClaw ban (Feb 2026) closes off cloud-only zero-install via subscription tokens; MCP sampling isn't shipped in Claude Code (use `claude -p --resume` per turn instead); Codex has full plugin/MCP/`exec resume` parity to mirror the Claude shape. New memory `project_closed_source_pivot.md` captures the full v1/v2 commitment; `project_oss_ethos.md` marked superseded; `project_v1_track_a_only.md` updated for new product framing. Both repos (`1-800-operator/operator` snapshot v2, personal `dufis1/operator`) stay private indefinitely.

**Exact next step (session 184): Begin Phase A1 — Switchboard skeleton.** Concretely: pick the web stack (Next.js? Remix? something simpler?), spin up `1-800-operator.com` as a real web app (replacing the Cloudflare Pages OSS-install-landing direction from session 182), implement Sign-in-with-Google, scaffold a database (Postgres on Fly.io? Supabase? Neon?) for users + sessions + per-user config + meeting metadata. After A1 lands, A2 (Fly.io headless Chrome browser fleet — port the Playwright connector code from `src/_1_800_operator/connectors/macos_adapter.py`), then A3 (the `@operator/bridge` Claude Code plugin wrapping `mcp_servers/transcript_server.py` + a new bridge MCP). Phase plan A1–A9 lives in `docs/product-strategy.md`.

**Open questions/blockers:**
1. **Web stack and infra picks for A1.** No commitment yet. Should be the first conversation when picking up next session.
2. **Free tier hours threshold.** Likely 2–5/meeting-hours/month, lock at launch — needs real usage feel first.
3. **Engine extraction strategy.** The OSS modules that port forward (`connectors/macos_adapter.py`, `pipeline/chat_runner.py`, `pipeline/mcp_client.py`, `pipeline/meeting_record.py`, `pipeline/guardrails.py`, `mcp_servers/transcript_server.py`) need a refactor plan — pulled into a shared library? Vendored separately into v1 cloud orchestrator + v1 plugin? Decide before A2/A3.
4. **OSS carry-overs all dropped.** PyPI bump, install.sh QA, second-Mac QA, Cloudflare Pages, all S177/S178 nits, 5 OPERATOR_BOT test files — all OSS-distribution debt that dies with the archive. Codex R8 captions + outbound MCP sync absorbed into A7 invariants. Nothing OSS-side left to finish.
5. **OSS repo archival timing.** Phase A9 (at v1 launch). No action needed pre-launch since both repos are already private.
