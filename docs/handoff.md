# Session 211 handoff (2026-05-10) — Phase 14.22.3 + 14.22.3.5 landed: naked-spawn architecture in claude_cli.py + heartbeat side-channel deleted + invariant audit clean. Net diff: +1138 / −1818 across 13 files. All 15 tests pass. Single code commit `4a55313`.

S211 was a heads-down implementation session. Started by hashing out scope: confirmed 14.22.3 was next on the roadmap, walked the user through what `claude_cli.py`'s long-lived persistent subprocess shape did, mapped the four pieces that `--append-system-prompt` was carrying (persona/voice, `_PRE_TOOL_VOICE_RULE`, transcript-tool backstop, --yolo denial hint), then surfaced and resolved four cross-cutting questions before writing code: (1) mid-stream failure UX → switchboard-operator framing where operator narrates connection drops + retry status in its own voice via a new `[☎️ Operator]` prefix, no harness-imposed turn timeout or heartbeat watchdog; (2) Anthropic detection vector → spawn signature is what carries harness identity, what operator reads OUT of stdout is unrestricted, system-prompt steering must move to SKILL.md content (client-side) or MCP tool descriptions, captured this principle as durable memory in `project_anthropic_detection_vector.md`; (3) `progress_callback` survives because it's stream-reading observability not spawn-signature impact; (4) bundled Chromium can be dropped from `install.sh` after 14.22.4 since slip's `connect_over_cdp` doesn't need it (folded into 14.22.5).

Implementation expanded scope mid-flight when surface mapping revealed the heartbeat side-channel in `chat_runner.py:_heartbeat_loop` — a daemon thread that spawned its own `subprocess.run(["claude", "-p", prompt, ...])` with an operator-authored prompt to make claude write a status sentence. Architecturally the same harness pattern we were stripping. User chose atomic-commit scope: 14.22.3 grew to include the heartbeat replacement. Replaced wholesale with operator-side narration via four wired callbacks (`progress` 20s-throttled, `denial` per-turn-deduped, `connection` for EOF + retry events, `tick` for off-thread send-queue drain). `_narrate_failure` also rewritten to direct operator-voice posts since its old shape spawned a `claude -p` with an operator-authored failure-narration prompt. AttachAdapter gained `send_chat_raw()` so operator-voice posts bypass the slip `[🤖 Claude]` prefix. After 14.22.3 landed, user requested a dedicated audit step (14.22.3.5) — six-vector sweep that found one more violation (`LLMClient.intro` authored a structured greeting prompt; slip never called it via `quiet_mode=True` but pattern existed); deleted along with `ChatRunner._generate_intro` + `_advance_intro_state` + intro-related state. Audit invariant now pinned by a test that asserts neither `--append-system-prompt` nor `--mcp-config` appear in the spawn cmd-array.

## Exact next step (session 212)

**Start Phase 14.22.4 — delete dial / deploy / login + adapters + chat-permission plumbing.** Files to remove: `connectors/macos_adapter.py`, `connectors/linux_adapter.py`, `pipeline/permission_bridge.py`, `pipeline/permission_chat_handler.py`, `pipeline/google_signin.py` (only used by `_run_login`), the `_run_dial` / `_run_deploy` / `_run_login` / `_run_macos` / `_run_linux` functions in `__main__.py`, `~/.operator/auth_state.json` + `~/.operator/google_account.json` plumbing, dial/deploy/login tests. Update `operator doctor` to remove dial/deploy-only checks (auth_state.json, Chrome-with-Google-cookies). Update `bridges/claude.py` to drop dial/deploy-only constants if any. Update `CLAUDE.md` "What This Project Does" + "Commands" sections to slip-only. Then 14.22.5 audit pass — already pre-loaded with the Chromium-drop spike result so install.sh + doctor + install_preflight chromium-install nag should land in that step. ~2-3h for 14.22.4.

## Open questions / blockers

- None blocking.

## Don't forget

- **Logging changed this session.** New `[☎️ Operator]` chat-post paths via `_narrate_*` methods + ChatRunner's stop-during-shutdown skip log. Removed: heartbeat loop logs, restart-after-death warnings, MCP-config tempdir creation logs. `debug/model-log.md` still doesn't exist (carry-forward since 14.12 / S167) so there's nothing to diff against — no-op for this session, but if model-log gets created later, this is the diff to capture.
- **The 14.22.3 commit deleted load-bearing-looking code.** Future sessions reading `git log` will see `_PRE_TOOL_VOICE_RULE` (S187), heartbeat (S206), `_restart_after_death` (S187 spike), transcript-MCP backstop (S188), `LLMClient.intro` (early architecture), pre-intro buffering machinery — all gone from main. The work survives in history but isn't current. Treat the "Prior Status (preserved)" sections in agent-context.md as the journey log.
- **Untracked working-tree items NOT touched this session** (carry over): `debug/14_20_audio_spike/{0, DECISION.md, STT_COMPARISON.md, USER_NOTE.md, decode_frames.py, spike_capture, spike_capture.swift}`, `debug/14_21_mic_capture_spike/spike_mic_via_sckit`, `debug/resume_spike/`, `docs/landing-page.md`, `public/`, `operator-architecture-handoff.md` at repo root.
- **Two unpushed commits at end of session** (`4a55313` S211 code + the docs sweep landing now). Push at start of S212 or at any clean point.
- **No live-meeting validation yet.** All test coverage is unit/integration; live `operator slip claude <url>` against a real Meet hasn't run since S209 (which was the BT HFP fix). Per the roadmap, end-to-end live test is Phase 14.22.10 — gates phase complete. Don't validate in isolation; the dial/deploy deletion in 14.22.4 will reduce the surface significantly so live-test once after that.
