# Session 210 handoff (2026-05-09/10) — V1 architecture pivot landed in roadmap: plugin form factor + naked `claude -p --resume` shellouts + slip-only product surface; no code changed

S210 was research + spike + decisions + roadmap restructure. Started by pushing the 9 unpushed commits to `origin/main` (handoff said 8; S209 docs sweep was the 9th) and updating the now-stale `project_ultrareview_gated_on_slip_captions.md` memory since the mic-leg E2E gate that gated `/ultrareview` and `/security-review` closed in S209. User then ran a Claude Code Research-mode report in their desktop app on Claude wrapper / meeting-bot complaints, brought back an external `operator-architecture-handoff.md` (untracked, at repo root) — kept ~half of its recommendations, walked back the other half via direct corrections. Locked architecture: plugin defaults to shared-context (slash command always passes `--resume-session ${CLAUDE_SESSION_ID}` to operator binary; ONE session end-to-end — the user's Claude Code session IS the meeting brain); strip ALL flags from inner-claude spawn (no `--append-system-prompt`, no `--mcp-config`, no `_PRE_TOOL_VOICE_RULE`); transcript MCP via `claude mcp add transcript --scope user --` at install time; slip-only — dial/deploy/login/MacOSAdapter/LinuxAdapter/auth_state.json/permission_bridge.py all DELETED; no Anthropic outreach; no Chrome extension (preserve slip CDP); no `--allowedTools`; `--yolo` preserved; no in-meeting announcement; self-host marketplace fallback if Anthropic plugin-directory review delays. Spike validated four assumptions (plugin .mcp.json scope, ${CLAUDE_SESSION_ID} substitution token, --resume+stream-json composes, nohup&-survives-Bash-tool empirically — PID reparented to PID 1, alive across 24s + multiple Bash returns). Plus submission-process spike: ZERO long-lead prereqs (no publisher account, no identity verification — unlike Apple Dev's 2-week onboarding), submission form open at `claude.ai/settings/plugins/submit`, 1-4 week manual review (no published SLA), skills get namespaced (`/operator:slip`). Roadmap edits: inserted **Phase 14.22 — Plugin form factor + naked `claude -p` shellouts** with 10 sub-steps (14.22.1 + 14.22.2 ✅ shipped this session); supersession banner on Phase 14.19 (steps 7F/G/H + 8 + 9 + 10 + 11 → 14.22); promotion banner on Post-MVP "Claude Code Plugin" section pointing at 14.22; reduced Phase 16.1 to "final polish" since substantive README work moved to 14.22.9. Plus added **Phase 14.21.4** — hide audio helper from Dock + Cmd-Tab via `LSUIElement = true` in Info.plist, rebuild + re-notarize (~1h, cdhash changes so first run after re-build re-prompts TCC; pair timing with 14.21.2's pre-prompt). Saved `memory/project_v1_architecture_pivot_s210.md` capturing decisions + spike findings. **MVP launch total: ~28-35h focused work (≈4-5 working days) + 1-4 week Anthropic plugin review running in parallel from 14.22.8.** Zero code changed; only `docs/roadmap.md` + `docs/handoff.md` + `docs/agent-context.md` modified, plus the new memory file. Zero unpushed commits at end of session (pushed the 9 at start, then only docs edits — those land in the end-session commit).

## Exact next step (session 211)

**Start Phase 14.22.3 — rewrite `claude_cli.py` from long-lived persistent subprocess to per-@mention `--resume` shellout.** Strip `--append-system-prompt`, strip `--mcp-config` + `_maybe_write_mcp_config`, strip the `_PRE_TOOL_VOICE_RULE` + `_PRE_TOOL_VOICE_RULE_YOLO_OVERRIDE` blocks. Add new `--resume-session <id>` CLI flag on `operator slip claude` that pre-populates `_session_id` so the very first @mention uses `--resume` (plugin always passes this; terminal-direct invocation omits it). Switch from long-lived persistent subprocess to per-@mention spawn-and-exit. ~3-4h. **After 14.22.3 + 14.22.4 (dial/deploy deletion) land, 14.22.5 is an explicit dead-code audit pass — user asked specifically for this so we don't leave orphans behind: grep every removed function/class/constant name, walk `tests/` for files importing deleted symbols, walk `docs/` for stale references in forward-facing docs (CLAUDE.md, agent-context "current state", roadmap.md, README.md), verify net LOC reduction matches expectation, all 15 tests still pass.**

## Open questions / blockers

- **API key rotation pending** from S205 — user reminded to revoke ANTHROPIC + OPENAI keys at console; not blocking.
- **Plugin submission timing.** Submit early after 14.22.7 lands; 1-4 week review window is the long pole. Self-host marketplace at `github.com/1-800-operator/operator-plugins` ready as fallback per 14.22.8 plan.
- None other blocking.

## Don't forget

- **The pivot kills entire phases of "shipped" code.** 14.19's three-rung ladder (slip survives but dial/deploy/login die in 14.22.4); the chat-bridged permission flow already deleted in S208 stays deleted; the `_PRE_TOOL_VOICE_RULE` system-prompt steering currently in `claude_cli.py` gets stripped because naked subprocess can't carry it. Future session reading the commit history will see commits like S193's "Phase 14.19.4 operator login claude" — that work survives in `git log` but the login command itself disappears in 14.22.4. Treat S193-S208 commits as steps in a longer arc that S210's pivot supersedes; agent-context.md "Prior Status" sections preserve the journey for narrative continuity.
- **`debug/model-log.md` still doesn't exist** (carry-forward since 14.12 / S167). Not blocking.
- **Untracked working-tree items NOT touched this session** (carry over): `debug/14_20_audio_spike/{0, DECISION.md, STT_COMPARISON.md, USER_NOTE.md, decode_frames.py, spike_capture, spike_capture.swift}`, `debug/14_21_mic_capture_spike/spike_mic_via_sckit`, `debug/resume_spike/`, `docs/landing-page.md`, `public/`, plus new `operator-architecture-handoff.md` at repo root (the desktop Claude's pivot doc — keep untracked, it's the source artifact for this session).
- **`/ultrareview` + `/security-review` are no longer gated** (mic-leg E2E closed in S209; ultrareview memory updated this session to reflect that). Could run either one before launch — though probably wait until after the 14.22 architecture pivot lands so the review covers the actual v1 shape.
