# Session 182 handoff (2026-05-03) — public repo ships via orphan-snapshot flow

This session shipped Phase 14.13.1 (public repo creation) and Phase 14.5 (`install.sh`). First attempt — `gh repo create 1-800-operator/operator --private` then `git push -u origin main` — exposed dev history files the user didn't want online; user deleted the repo (`gh auth refresh -h github.com -s delete_repo` was needed because the default `gh` token is missing that scope), and we pivoted to a curated **orphan-snapshot publish flow**: `git checkout --orphan public-snapshot-vN` → `git rm -rf --cached .` → `git add` an explicit 11-item ship list (`.gitignore`, `LICENSE`, `README.md`, `SECURITY.md`, `pyproject.toml`, `requirements.txt`, `operator`, `install.sh`, `_redirects`, `src/`, `tests/`) → force-push to `public main`. Three snapshots landed: v0 (initial), v1 (added `install.sh` — 138 lines, idempotent, covers Python preflight + uv bootstrap + `uv tool install git+URL` + `playwright install chromium` + `~/.operator/.env` seeding + macOS Chrome cask nudge), v2 (added `_redirects` for Cloudflare Pages's `/install /install.sh 200` rewrite). Two local commits on `main`: `d646008` and `18e028f`. Public repo HEAD is `058b4f5`. **Repo flipped private at end of session** as a deliberate pause-state.

**Exact next step (session 183):** Flip the repo public again:
```
gh repo edit 1-800-operator/operator --visibility public --accept-visibility-change-consequences
```
Then drive Cloudflare Pages dashboard steps 2–3 (Connect to Git → `1-800-operator/operator` → main / framework None / build command empty / build output `/`; then Custom domains → add `1-800-operator.com`). Then run verification curls (`curl -I https://1-800-operator.com/install`, `curl https://1-800-operator.com/install | head -20`) — Claude can run those once the user reports the Pages project is wired up. Then **second-Mac QA** of the `curl | sh` flow end-to-end (install completes → Chromium downloads → `.env` seeded → Chrome nudge fires or doesn't → `operator` on PATH → `operator setup` runs → `operator dial claude <fresh-meet-url>` joins a real meeting). Iterate any QA tweaks → re-snapshot via the orphan procedure → re-test. Then Phase 14.13.4 (archive `dufis1/operator`) → Phase 16 (README rewrite mentioning both claude + codex, demo GIF, landing site, launch).

**Open carry-overs:**
1. **Repo must be public for QA.** `uv tool install git+https://github.com/...` resolves via anonymous git clone. Cloudflare Pages CAN deploy from private repos via its GitHub App, but the `curl | sh` install flow itself needs the repo public.
2. **Re-snapshot procedure** (use any time `install.sh`, `_redirects`, or any other ship-list file changes): `git add <changed files> && git commit -m "..."` (on `main`), then `git checkout --orphan public-snapshot-vN` → `git rm -rf --cached .` → `git add .gitignore LICENSE README.md SECURITY.md pyproject.toml requirements.txt operator install.sh _redirects src/ tests/` → `git commit -m "Release vN"` → `git push -f public public-snapshot-vN:main` → `git checkout -f main`. The `-f` on the final checkout is safe because the unstaged-not-modified files on the orphan are bit-identical to `main`'s tracked content.
3. **Pages shape A vs B.** Currently planning shape A (Pages deploys operator public repo, output `/`). Shape B (separate `1-800-operator/landing` repo with just install.sh duplicated) is cleaner but not needed until Phase 16 lands a real `index.html`.
4. **install.sh end-to-end is unverified.** Bash syntax, git URL resolution, Python detection one-liners all confirmed locally. The actual `uv tool install` → `playwright install chromium` → working `operator` binary chain is what the second-Mac QA exists to validate. If the `uv tool run --from "git+..." playwright install chromium` step fails (it might create a separate ephemeral env from the install one), fall back to running `playwright` from `$(uv tool dir)/1-800-operator/bin/playwright` directly.
5. **Old carry-overs unchanged from session 181:** PyPI bump from v0.0.1 (post-launch), codex R8 caption parity + outbound MCP-import sync from `~/.codex/config.toml` (post-launch), tier-2 audit + ~70 appendix nits from `docs/code-quality-audit-session-178.md` (post-launch), S177 nits (`_do_send_chat` ID-readback race, `MeetingRecord.append` memory-vs-disk divergence, `_on_tool_use` docstring drift) (post-launch), 5 test files needing in-file `OPERATOR_BOT` (post-launch).
