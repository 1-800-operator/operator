# Session 190 handoff (2026-05-04) — Phase 14.8 install path A verified end-to-end on a fresh Mac

## What landed

Thirteen fix commits, all driven by real QA against the public install URL on a never-used MacBook. End state: first successful `operator dial claude` on a fresh machine, with `meet.new` redirecting cleanly to a real meeting URL via the green-room flow. Highlights: install.sh now provisions Python 3.12 via uv when the system Python is below 3.10 (macOS ships 3.9, so this was hard-blocking); `pyyaml` added as a direct dep (was being pulled in transitively in dev but missing from a fresh `uv tool install`); curl one-liner flipped from `| sh` to `| bash` everywhere (script shebang is bash); installer sendoff redesigned (green ✓ + bold "operator successfully installed!" + Docs line + bold-magenta `Next:` line that auto-prefixes `source ~/.local/bin/env &&` when needed); wizard step 1 polished (heading prints once, single-line warning, exits cleanly on prereq-gate failure instead of re-prompting); 4 bundled skills culled (codebase-walkthrough, design-review-feedback, live-bug-triage, migration-plan); install.sh playwright invocation reverted from `--from playwright` to `--from "git+${REPO_URL}"` (the silenced cosmetic warning was load-bearing — `--from playwright` resolved latest PyPI playwright and downloaded a chromium build operator's pinned 1.58.0 couldn't find); Chromium args got `--use-fake-device-for-media-stream` to sidestep the macOS mic-permission prompt; new dial-time preflight hard-exits if `~/.operator/browser_profile/` is missing (saves a 30s hang on a misleading "did not redirect" error when sign-in wasn't done); save_debug wired into the meet.new redirect-timeout path so failures get captured. **The clincher:** meet.new sometimes sits on `/new` showing a "Do you want people to see and hear you in the meeting?" interstitial with a "Continue without microphone and camera" button — instead of redirecting straight to a meeting URL. The adapter already had logic to click that button, but it ran AFTER `wait_for_url(_is_real_meet_room)` succeeded. Chicken-and-egg, since the URL is held at `/new` until clicked. Replaced the single wait_for_url with a 30s poll that watches for both — clicks the button when it appears, redirect completes. All thirteen commits landed on `origin/main` (private dev) and `public/main` (1-800-operator/operator) via the surgical worktree-push procedure (no orphan-snapshot dance — direct file mirror, fast iteration).

## Exact next step (session 191)

Two reasonable directions:

1. **Finish Phase 14.8 — remaining install paths + live-meeting test.** Path A is verified; paths B (`curl → less → bash`) and C (`uv tool install git+... + operator setup`) untested. The install_preflight module from S189 should make path C work cleanly, but it hasn't been exercised on a fresh machine. After install paths are clean, run the actual claude top-10 use-case live-test against a real Meet — that's the marathon. Phase 14.8 sub-bullets (d), (e), (f) are still ⬜ in the roadmap.

2. **Phase 16.1 README rewrite.** Per Phase 16.1 spec — hero positioning ("Claude Code in your Google Meet"), agents-gallery framing, demo-GIF placement, MCP compatibility notes, one annotated example config. The S189 README documents three install paths cleanly but doesn't have launch-tier polish.

The user closed session 190 with a successful first dial; obvious "let's verify everything else" path is more 14.8 sub-items. README rewrite is the bigger pre-launch lift but doesn't gate on more QA.

## Open questions / blockers

- **Dial-time `[claude] ⚠ MCP needs attention` warnings** still fire on the test Mac for hosted connectors (Gmail, Calendar, Drive). The hint text now points at `operator auth <slug>`, but it's unclear whether running that actually clears the warning — the warning's source is `claude mcp list`, which reads Claude Code's account state, not operator's mcp-remote bridge state. Possibly a deeper diagnostic-vs-actionable mismatch worth untangling. Not blocking — the dial proceeds.
- **The 4 hosted MCPs the test Mac sees come from the user's cloud claude.ai account**, not from this machine. Worth flagging in the README's claude-preset section so users aren't confused about provenance ("why does my fresh laptop already know about Gmail?").
- **`uv tool upgrade 1-800-operator` on power-user systems would skip install.sh's chromium-download step** if a future operator version bumps the playwright pin. Latent footgun; worth wiring chromium-install into a runtime preflight in Phase 16.
- **`docs/handoff.md`, `debug/resume_spike/`, `docs/pre-launch-audit.md`** still untracked from prior sessions — leaving alone.
