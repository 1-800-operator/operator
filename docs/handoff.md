# Session 212 handoff (2026-05-10) — Phase 14.22.4 + 14.22.5 + 14.22.6 shipped: dial / deploy / login deleted (-3894/+71); dead-code audit + bundled-Chromium drop + llm.py thin pass-through (-873/+79); install.sh registers transcript MCP user-scope. 11/11 tests green throughout.

S212 was a focused architecture-strip session. Three commits land slip-only across code + docs + installer. **14.22.4 (`9111859` + `1899916`)** deleted dial / deploy / login: six source files (macos_adapter, linux_adapter, captions_js, google_signin, chrome_preflight, transcript), four dial-only test files, the `_run_login`/`_run_macos`/`_run_linux`/`_run_bot` functions in `__main__.py`, `--force` flag, the `quiet_mode` parameter chain in `chat_runner.py` (slip is the only mode now; no 1-on-1 trigger bypass), dial-only constants in `config.py` + `connectors/session.py`, and dead failure-reason branches in chat_runner. CLAUDE.md rewritten slip-only with the post-14.22.3 corrections folded in (operator-voice narration is stream-reading callbacks not `_PRE_TOOL_VOICE_RULE`; naked-spawn invariant; no 1-on-1 bypass). `connectors/base.py` MeetingConnector kept as a seam for future bridges. **14.22.5 (`715b3b3`)** dead-code audit: dropped `playwright install chromium` from install.sh (slip's `connect_over_cdp` doesn't need bundled Chromium — saves ~170MB / 30-60s install time); deleted four dial-era spike scripts and two orphaned test probes; deleted the vestigial `_tail_messages`/`_build_messages` chain in `llm.py` (claude_cli only ever read `messages[-1]`); dropped `config.HISTORY_MESSAGES` and `LLMClient.ask`'s `record` parameter; swept stale comments in `attach_adapter.py` + `chat_dom_js.py` referencing deleted MacOSAdapter / LinuxAdapter / TranscriptFinalizer / dial paths. **14.22.6 (`6e46ca6`)** added the `claude mcp add transcript --scope user` step to install.sh (idempotent remove-then-add; soft-skip with hint when `claude` isn't on PATH yet; absolute uv-tool venv python path). Live-verified locally: first-run + idempotent re-run + soft-skip-when-claude-missing all green.

## Exact next step (session 213)

**Start Phase 14.22.7 — build the `operator-plugin` repo.** New repo at `github.com/1-800-operator/operator-plugin`. Layout: `.claude-plugin/plugin.json` (name `operator`, description, author, repository, homepage); `skills/slip/SKILL.md` (bash: `nohup operator slip claude --resume-session ${CLAUDE_SESSION_ID} $ARGUMENTS > /tmp/operator-current.log 2>&1 &`); `skills/status/SKILL.md` (bash: `operator status`); `skills/hangup/SKILL.md` (bash: `pkill -f "operator slip"`); `skills/doctor/SKILL.md` (bash: `operator doctor`); `README.md` pointing at the curl-script for the binary install. Add `operator status` + `operator hangup` to operator's CLI itself (slim subcommands reading the marker file or pkill'ing). 14.22.8 (Anthropic plugin directory submission + self-hosted marketplace fallback) ships in parallel since the 1-4 wk review window is the long pole. ~2-3h for 14.22.7.

## Open questions / blockers

- None blocking.

## Don't forget

- **Six unpushed commits ahead of `origin/main`**: `4a55313` (S211 14.22.3 + 14.22.3.5 code), `a055d8a` (S211 docs sweep), `9111859` (S212 14.22.4 code), `1899916` (S212 14.22.4 docs), `715b3b3` (S212 14.22.5), `6e46ca6` (S212 14.22.6). Push at start of S213 or any clean point.
- **No live-meeting validation yet.** End-to-end live `operator slip claude <url>` against a real Meet is gated to Phase 14.22.10. The 14.22.4 deletion plus today's 14.22.6 transcript MCP registration significantly changed the user-facing surface; live-test once after 14.22.7-9 land to catch any integration drift.
- **Logging changed this session.** 14.22.4 removed dial/deploy-specific log lines (Chrome SingletonLock, auth-state validation, MacOSAdapter / LinuxAdapter session lifecycle, kill-reason file, login flow). 14.22.6 added one new install-time stdout line (`Registered transcript MCP (user-scope).`) — installer output, not `/tmp/operator.log`. `debug/model-log.md` still doesn't exist (carry-forward since 14.12 / S167) so there's nothing to diff against.
- **`uv tool install --reinstall` needed before any live test.** Operator runs from the uv-tool install at `~/.local/share/uv/tools/1-800-operator/`, not the working tree. Today's three commits are NOT in the live install. The install.sh patch from 14.22.6 will register the transcript MCP only when install.sh re-runs — running `uv tool install --reinstall` alone won't pick it up. To live-test the full new chain, run install.sh end-to-end after pushing.
- **The 14.22.4 commit deleted load-bearing-looking code.** Future sessions reading `git log` will see `_run_login`, `MacOSAdapter`, `LinuxAdapter`, `TranscriptFinalizer`, `quiet_mode`, `_tail_messages`, `chrome_preflight`, the SingletonLock helpers, `auth_state.json` plumbing — all gone from main. The work survives in history but isn't current. Treat the "Prior Status (preserved)" sections in agent-context as the journey log.
- **Untracked working-tree items NOT touched this session** (carry over): `debug/14_20_audio_spike/{0, DECISION.md, STT_COMPARISON.md, USER_NOTE.md, decode_frames.py, spike_capture, spike_capture.swift}`, `debug/14_21_mic_capture_spike/spike_mic_via_sckit`, `debug/resume_spike/`, `docs/landing-page.md`, `public/`, `operator-architecture-handoff.md` at repo root.
