# Session 214 handoff (2026-05-10) — Phase 14.22.8 shipped: self-hosted marketplace fallback inside the CLI repo + 14.22.9 ↔ 14.22.10 reorder so live-test precedes README + Anthropic directory submission. Two commits on `main` (`3cc9d7f` marketplace + 14.22.8 row, `ca94414` reorder + cross-references), both pushed.

S214 dropped a `.claude-plugin/marketplace.json` at the operator CLI repo root (option b — chosen over a third repo or the plugin repo: one fewer repo to maintain, install path is co-located with the CLI users are already looking at, and the plugin repo flips public at 14.22.10 anyway). Marketplace name `1-800-operator`; plugin entry sources `github.com/1-800-operator/operator-plugin` pinned to `ref: "main"` for fast beta iteration; mirrors plugin.json metadata. `claude plugin validate` passes clean (after one warning fix — top-level `description`). Local end-to-end smoke: `claude plugin marketplace add /Users/jojo/Desktop/operator` → ✔ added → ✔ removed cleanly. Beta testers will run `/plugin marketplace add 1-800-operator/operator` + `/plugin install operator@1-800-operator` (need plugin-repo collaborator access until 14.22.10 flips it public). Then user pushed back on the original 14.22.9 → 14.22.10 sequence (README → live-test) noting that QA + live testing must precede docs polish + Anthropic directory submission. Agreed and swapped: 14.22.9 = live-test (1-2h), 14.22.10 = README + landing-page + launch tasks (flip plugin repo public, tag-pin marketplace, submit to Anthropic directory) (3-4h). README **intentionally not touched** this session — it's heavily stale (still describes `operator dial`, `operator setup`, per-bot YAML, Playwright Chromium, `~/.operator/.env` API keys); the clean rewrite is scoped to 14.22.10 to avoid a half-fix.

## Exact next step (session 215)

**Start Phase 14.22.9 — live-test plugin → meeting end-to-end.** On a fresh Mac (or via `~/.operator` reset): `curl | sh` install → `/plugin install operator@1-800-operator` (tester needs collaborator access on the still-private plugin repo, OR flip plugin repo public early via `gh repo edit 1-800-operator/operator-plugin --visibility public --accept-visibility-change-consequences`) → open a Claude Code session and seed it with prep context (paste a Linear ticket, draft an idea) → `/operator slip <meet-url>` → join Meet → `@claude what's that ticket I was reading earlier?` → confirm pre-meeting context bridged correctly via `--resume` → ask follow-up that exercises the transcript MCP (`@claude what did we just discuss?`) → leave Meet → return to the Claude Code session, confirm meeting interactions are visible in conversation history → debrief in same session works. Then test terminal-direct path: `operator slip claude <url>` (no `--resume-session` flag) → confirm fresh session is born on first @mention + `--resume` thereafter. Any bugs surfaced loop back to whichever earlier phase owns the surface (CLI / plugin / install.sh / marketplace.json) before proceeding to 14.22.10. ~1-2h.

## Open questions / blockers

- **Plugin repo visibility decision before S215 starts.** Either add testers as collaborators on the still-private `1-800-operator/operator-plugin` (preserves the 14.22.10-defined launch sequence), or flip the plugin repo public early (simpler tester onboarding, but the public README still reads as if launch already happened — `1-800-operator.com/install.sh` curl-script and `/plugin install operator@1-800-operator` are documented as if real, even though the curl-script's hosting and the public marketplace are not yet up). Recommend the collaborator path: keeps the launch sequencing clean.
- **Live-test prerequisites:** the user has done all the underlying work (mic-leg E2E closed in S209, naked-spawn invariant in 14.22.3, transcript MCP user-scope registration in 14.22.6, plugin scaffold in 14.22.7, marketplace fallback in 14.22.8). 14.22.9 is the first end-to-end exercise of all of it together. There's a non-trivial chance something fails at the install / discovery / spawn boundaries that didn't show up in component tests.

## Don't forget

- **Zero unpushed commits.** Both S214 commits (`3cc9d7f`, `ca94414`) pushed at session end.
- **`uv tool install --reinstall` needed before live-test.** Operator runs from the uv-tool install at `~/.local/share/uv/tools/1-800-operator/`, not the working tree. The S213 `__main__.py` patch (status + hangup subcommands) is on `main` but not in the live install. Reinstall before live-test or the plugin's status/hangup skills will fail.
- **Plugin repo is PRIVATE** (`gh repo view 1-800-operator/operator-plugin --json visibility`). Marketplace.json sources it; install will fail for non-collaborators.
- **Marketplace `ref: "main"`** means every push to the plugin repo's main branch reaches subscribers via `/plugin marketplace update`. Acceptable for beta; 14.22.10 flips this to a tag pin (`v0.1.0`) before public launch.
- **README is heavily stale.** Do NOT add fragments to it before 14.22.10's full rewrite. `operator dial`, `operator setup`, per-bot YAML, Playwright Chromium, `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` references all need to come out together with new copy that reflects the slip-only + plugin-install reality.
- **No logging changes this session.** No new lines in `/tmp/operator.log`. `debug/model-log.md` still doesn't exist (carry-forward since 14.12 / S167).
- **Untracked working-tree items NOT touched this session** (carry over): `debug/14_20_audio_spike/{0, DECISION.md, STT_COMPARISON.md, USER_NOTE.md, decode_frames.py, spike_capture, spike_capture.swift}`, `debug/14_21_mic_capture_spike/spike_mic_via_sckit`, `debug/resume_spike/`, `docs/landing-page.md`, `mvp.md`, `public/`, `operator-architecture-handoff.md` at repo root.
