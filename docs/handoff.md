# Session 208 handoff (2026-05-07) — chat-bridged permission gate ripped out wholesale; operator now defers to Claude Code's native permissions

Started as a debug session — slip mode hanging when inner-claude tried a write tool. Diagnosed `greenlet.error: Cannot switch to a different thread` from `PermissionChatHandler` calling Playwright's sync API on the provider's permission-pump thread. Patched four threading layers (gate hand-off via `_PendingPermissionGate`, provider `_stopping` flag for clean shutdown, off-thread send queue for heartbeats, AttachAdapter browser-thread queue mirroring the MacOSAdapter/LinuxAdapter pattern), plus an optimistic-ID filter at `AttachAdapter._do_read_chat` (Meet renders a numeric placeholder before the canonical `spaces/.../messages/...` ID; both fired the observer, double-dispatching every slip-mode user message). **Then user pushed back on the premise itself** — "Why are we adding a layer of permissions when Claude has his own permissions?" Right call. The chat-permission flow was added in 14.19.8 (May 5) as a Claude judgment about safety, not user-driven. Ripped it out: ~1058 net LOC deletion, 5 file deletions, single-tier declarative narration stays. Two commits on `main`: `7d0c8f0` (deletion + threading-fix arc kept as defensive layers since AttachAdapter being thread-safe is a correctness fix unrelated to permissions) and `33f5bba` (`DENIAL RECOVERY` follow-up — when Claude Code denies a tool, model now suggests re-running with `--yolo` in the same reply). **Live-validated end-to-end across two slip sessions today**, including a clean `Created PRO-110 "Do laundry"` Linear issue create in run 2 once the user added `mcp__linear__*` to their `~/.claude/settings.json`. 15/15 tests pass (was 17 — lost the two permission-flow tests). Saved feedback memory `feedback_dont_invent_safety_scaffolding.md` so I don't repeat this pattern. Six commits ahead of `origin/main` at end of session.

## Exact next step (session 209)

Pick up the mic-leg E2E test that was carry-forward from S207 — the system-audio leg validated successfully today, mic leg remained. User comes back to the desk, unmutes the mic, speaks as a "reply," then `@claude what did the other person say?` to close the transcript-MCP round-trip. The `[M]` audio stream itself was demonstrated working in every prior failure run (helper logs `[M]` callbacks streaming reliably across all attempts) — only the `@claude` round-trip is genuinely untested. After that: (a) Phase 14.21.1 (mic-mute privacy gate, ~3h, real privacy bug — Meet's mute toggle does not silence the helper's `M` stream because the helper taps the system mic device directly), (b) Phase 14.21.2 (TCC pre-prompt UX + doctor probes, ~2h), (c) push the six local commits ahead of `origin/main` (`51b69f3`, `a0aa727`, `66ab455`, `cdbe442`, `7d0c8f0`, `33f5bba`), or (d) close the `pre-launch-audit.md` revisit/wontfix log entry for the install_preflight + readiness orphans (S206 carry-forward). `/ultrareview` + `/security-review` remain gated on the mic-leg validation per memory.

## Open questions / blockers

- **API key rotation pending** from S205 second-pass — user reminded to revoke ANTHROPIC + OPENAI keys at console; not blocking, hygiene only (operator no longer consumes either).
- **Mic-leg E2E** untested — gates `/ultrareview` + `/security-review`.
- **`debug/model-log.md` may need updates.** This session added/removed several log lines. Removed: `PermissionChatHandler: ...`, `ClaudeCLI permission bridge: tempdir=...`, `ClaudeCLI permission pump started`, `routing reply to permission gate: ...`. Added: `ChatRunner: provider wired (no permission gate; Claude Code defaults apply)`, `AttachAdapter: dropping placeholder-id message id=...`, `ClaudeCLI: streaming turn aborted during shutdown — propagating`, `ClaudeCLI: shutdown in progress — not restarting subprocess`, `ChatRunner: skipping failure narration — shutdown in progress`. End-session skill flagged this; deferred to S209.
- **Apple Dev cert** carry-forward note can be retired (S207 noted 14.20.5 substantively done with today's notarized .app).

## Don't forget

- **The S207 handoff worried about an "uncommitted refactor in working tree authored by a parallel agent"** — that worry is now closed. S208's begin-session `git status` showed only `M docs/handoff.md` plus the same untracked debug/landing-page items S206 carried over (no `M` lines for any permission files). Either the parallel-agent's changes were reverted before S208 began, or S208 made the equivalent changes afresh under full user direction in this conversation. Either way, the work is now committed (`7d0c8f0` + `33f5bba`) with clear authorship.
- **Untracked working-tree items NOT touched this session** (carried from S206 and S207): `debug/14_20_audio_spike/{0, DECISION.md, STT_COMPARISON.md, USER_NOTE.md, decode_frames.py, spike_capture, spike_capture.swift}`, `debug/resume_spike/`, `docs/landing-page.md`, `public/`. Carry over.
- **Heartbeats during permission gates were a misleading-progress UX** — the heartbeat side-channel said "Pulling up the most recent item now and filling in the rest" while the bot was actually blocked on a chat-side gate. With the gate gone, this confusion is impossible — heartbeats only fire during legitimate model silence (no chained text output). If heartbeats need any further adjustment, the model is now in a much simpler position to author them.
- **The `DENIAL RECOVERY` prompt rule was added AFTER both validation runs today** — next time a user hits a permission denial in non-yolo mode, that's the first live test of whether the model actually surfaces the `--yolo` suggestion. Watch the reply text.
- **Carry-forward from S207** (paraphrased — full detail in agent-context.md "Prior Status (preserved)"): voice-preserved's whisper-base matches current operator exactly (no regression on the "Hosting" mishearing of "testing" — that's whisper-base's character on short low-context phrases); don't bump to `small` until 3-5 real-meeting samples justify it.
