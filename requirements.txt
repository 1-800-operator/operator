# ── Cross-platform dependencies (Phase 14.2: pinned for reproducible installs) ─

python-dotenv==1.2.2   # loads .env into operator's process env (third-party MCP creds, not LLM auth)
playwright==1.58.0     # controls headless/headed Chrome for joining Google Meet
mcp==1.27.0            # Model Context Protocol — used by bundled transcript MCP server
pyyaml==6.0.3          # YAML — used by tests; harmless at runtime
rich==14.3.3           # terminal UI — used by google_signin's prompts and status output

# ── Audio pipeline (Phase 14.20.4, S233 backend swap) ─────────────────────────
# Local Whisper STT for dial mode. CTranslate2-based; runs CPU-only with no
# Metal/MLX dependency. S233 swapped from mlx-whisper after two production
# crashes from MLX's async Metal command-buffer abort path.
faster-whisper==1.2.1
numpy==2.4.3

# ── Transitive-dep pins (pre-launch v0.0.1) ──────────────────────────────────
# Locked to exact versions for reproducible installs. Post-launch the security
# floors may relax back to `>=` so CVE patches land via `pip install -U` once
# we have an upgrade-intent flow. Mirror pyproject.toml exactly.
cryptography==48.0.0     # via mcp → pyjwt[crypto]
pygments==2.20.0         # via rich
python-multipart==0.0.29 # via mcp
