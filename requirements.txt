# ── Cross-platform dependencies (Phase 14.2: pinned for reproducible installs) ─

python-dotenv==1.2.2   # loads API keys from the .env file into the app
openai==2.29.0         # OpenAI SDK — kept pending second-provider product call
anthropic==0.94.0      # Anthropic SDK — kept pending second-provider product call
playwright==1.58.0     # controls headless/headed Chrome for joining Google Meet
mcp==1.27.0            # Model Context Protocol — used by bundled transcript MCP server
pyyaml==6.0.3          # YAML — used by tests; harmless at runtime
rich==14.3.3           # terminal UI — used by google_signin's prompts and status output

# ── Audio pipeline (Phase 14.20.4) ────────────────────────────────────────────
# Local Whisper STT for slip mode. Mac-only at runtime; Linux dial doesn't import.
mlx-whisper==0.4.3
numpy==2.4.3

# ── Security floors for transitive deps (pip-audit, session 138) ──────────────
# Left as `>=` (not `==`) so future CVE patches land on `pip install -U` without
# a requirements edit. Bump the floor when a newer CVE ships.
cryptography>=46.0.7     # CVE-2026-34073, CVE-2026-39892 (via mcp → pyjwt[crypto])
pygments>=2.20.0         # CVE-2026-4539 (via rich)
python-multipart>=0.0.26 # CVE-2026-40347 (via mcp)
